# Write your MySQL query statement below
SELECT e1.employee_id, e1.name, count(e2.reports_to) as reports_count, round(avg(e2.age),0) as average_age FROM Employees e1, Employees e2 where e1.employee_id = e2.reports_to group by e2.reports_to order by e1.employee_id;
