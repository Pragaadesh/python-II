class Solution(object):
    def secondHighest(self, s):
        temp=[] # 1,2,3,2,1
        for i in s: 
            if i.isdigit() and int(i) not in temp:
                temp.append(int(i))  
            
        if len(temp)<2:
            return -1 
        else:
            temp.sort() 
            return temp[-2]
        
                
